# encode.txt
# List of components to be hashed for integrity but kept as open-source.
# The .py source files for these components WILL NOT be deleted or compiled.
# One component folder name per line.

# Simple logic modules
set_variable_module
if_module
keyword_router_module

# UI-related plugins that are safe to distribute as source
metrics_dashboard